/** Flat referee hire fee (VND) — matches spot-backend's non-prod default
 * used across the referee docs/examples (POST /bookings hireReferee). No
 * per-venue/per-sport pricing yet; backend accepts any refereeFeeVnd, this
 * is just the one the FE currently offers. */
export const REFEREE_FEE_VND = 150000;
