// Shared spacing scale for the onboarding/role flow, so the new
// choose-role/owner/referee/pending screens don't invent their own
// one-off magic numbers.
export const spacing = {
  xxs: 2,
  xs: 4,
  sm: 8,
  md: 16,
  lg: 24,
  xl: 32,
} as const;
